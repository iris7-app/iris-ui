# TASKS — iris-ui

Open work only. Per `~/.claude/CLAUDE.md` rules : UI-only items
here ; done items removed (use `git tag -l` for history).

---

_Aucune tâche ouverte. Dashboard.component.ts (670 LOC) et
customers.component.ts (457 LOC) sont sous le plancher 1000 LOC de
la règle file-length-hygiene → NE PAS splitter — la valeur du split
disparaît sous ce seuil._
